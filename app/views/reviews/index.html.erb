<%= render 'layouts/adminpage' %>

<% if @current_user && @current_user.is_admin %>
    <h1>Aggregated Reviews (not for public viewing)</h1>

    <% @reviews.each do |review| %>
      <p><%= link_to review.text, review %></p>
  <% end %>
<% end %>