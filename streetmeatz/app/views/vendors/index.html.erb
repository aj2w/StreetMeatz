<h2>Vendors Near <%= params[:search] %></h2>

<% @vendors.each do |vendor| %>
<%= link_to(vendor.name,vendor) %><br>
<% end %>

<%= gmaps4rails(@json) %>

<%= link_to "Add a new vendor", new_vendor_path %>